

truncate `global`;

truncate `server`;

truncate `user`;

truncate `whitelist`;

truncate `activationcode`;

truncate `rolesocial`;

truncate `auctionitem`;

truncate `playername`;

truncate `unionname`;